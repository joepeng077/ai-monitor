# 贡献说明

欢迎提交问题和改进。提交前请确保：

- 代码、文档、图标和截图均为原创或具备明确再分发许可；
- 不包含 API Key、Cookie、令牌、真实账户数据、日志、应用容器或构建产物；
- 不实现绕过登录、权限或产品访问控制的功能；
- 运行 `swift test` 和 README 中的 Xcode 构建命令。

涉及账户集成的改动必须默认关闭，并附带数据来源、条款依据、隐私影响和测试说明。
